import React from 'react';
import { styled } from 'styled-components';

type Props = {};

const DocumentMapDrawerContent = ({}: Props) => {
  return <></>;
};

export default DocumentMapDrawerContent;
